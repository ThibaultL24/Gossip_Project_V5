<!DOCTYPE html>
<html>
<head>
  <title>Bienvenue sur la page d'accueil</title>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Accueil</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
      <%= link_to "Users", users_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
      <%= link_to "Gossips", gossips_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
      <%= link_to "tags", tags_path, class: "nav-link" %>
      </li>
    </ul>

    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <%= link_to "Notre équipe", team_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Contact", contact_path, class: "nav-link" %>
      </li>
    </ul>
  </div>
</nav>

<div class="card-container">
  <% @gossips.each do |gossip| %>
    <div class="card" style="width: 18rem;">
      <div class="card-body">
        <h5 class="card-title"><%= link_to gossip.user.first_name, user_gossips_path(gossip.user) %></h5>
        <h6 class="card-subtitle mb-2 text-body-secondary"><%= gossip.title %></h6>
        <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
        <%= link_to "Voir les potins de #{gossip.user.first_name}", user_gossips_path(gossip.user) %>
      </div>
    </div>
  <% end %>
</div>

<% @gossips.each do |gossip| %>
    <div>
      <strong><%= link_to gossip.title, gossips_path(gossip) %></strong> 
      par <%= link_to gossip.user.first_name, users_path(gossip.user) %>
    </div>
  <% end %>

</body>
</html>
